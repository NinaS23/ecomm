{
  "$jsonSchema": {
    "title": "Record of categories",
    "description": "This document records the details of an categories",
    "type": "object",
    "properties": {
      "_id": {
        "description": "A unique identifier for an categorie",
        "bsonType": "objectId"
      },
      "nome": {
        "bsonType": "string",
        "minLength": 3
      },
      "status": {
        "bsonType": "string",
        "enum": [
          "ATIVA",
          "INATIVA"
        ]
      }
    },
    "required": [
      "nome",
      "status"
    ],
    "additionalProperties": false
  }
}